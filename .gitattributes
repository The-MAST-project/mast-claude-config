# Shell scripts must keep LF endings so they run on the Linux dev hosts,
# regardless of the committer's platform.
*.sh text eol=lf

# PowerShell is happy with either, but keep it consistent.
*.ps1 text eol=crlf
